var shell = new ActiveXObject("WScript.Shell");var progPath = Session.Property("CustomActionData");var fso = new ActiveXObject("Scripting.FileSystemObject");var shortcut = shell.CreateShortcut(fso.BuildPath(progPath,"CIE Middleware\\Cambio PIN.lnk"));var windir = fso.GetSpecialFolder(1); //system foldershortcut.TargetPath = fso.BuildPath(windir,"rundll32.exe");shortcut.Arguments = "CIEPKI.dll,CambioPIN";shortcut.IconLocation = "CIEPKI.dll";shortcut.Save();var shortcut2 = shell.CreateShortcut(fso.BuildPath(progPath,"CIE Middleware\\Sblocco PIN.lnk"));shortcut2.TargetPath = fso.BuildPath(windir,"rundll32.exe");shortcut2.Arguments = "CIEPKI.dll,SbloccoPIN";shortcut2.IconLocation = "CIEPKI.dll";shortcut2.Save();